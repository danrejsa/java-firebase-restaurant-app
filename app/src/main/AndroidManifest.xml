<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quanteq.white">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <supports-screens android:anyDensity="true" />

    <uses-permission android:name="android.permission.BLUETOOTH" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/z"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddFood" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login" />
        />
        <activity android:name=".Menupage" />
        <activity android:name=".Orderpage" />
        <activity android:name=".welcome" >

        </activity>
        <activity android:name=".viewOrders" >

        </activity>
        <activity android:name=".ResetPasswordActivity" />
        <activity
            android:name=".Q_Restaurant"
            android:label="@string/title_activity_q__restaurant"
            android:theme="@style/AppTheme">

        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".adminRegister" />
        <activity android:name=".adminLogin" />
        <activity android:name=".splashs"></activity>
        <activity android:name=".PageAdapter" />
        <activity android:name=".Nenupage" >

        </activity>
        <activity android:name=".AddDrink" />
        <activity android:name=".Add" />
        <activity android:name=".rateUs" />
        <activity android:name=".aboutUs" />
        <activity android:name=".feedback" />
        <activity android:name=".orderpageM" />
        <activity android:name=".orderpageD" />
        <activity android:name=".orderpageK" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".print" />
        <activity android:name=".POP" />
        <activity android:name=".orderRequestButtonn" />
        <activity
            android:name=".QrestaurantNavStart"
            android:label="@string/title_activity_qrestaurant_nav_start"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".History">

        </activity>
        <service android:name=".FirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <service android:name=".FirebaseInstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>